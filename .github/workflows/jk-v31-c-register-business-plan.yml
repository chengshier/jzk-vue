name: jk-v31-c-register-business-plan
on:
  push:
    branches: [feat/jzk-v3.1-phase3-operations]
    paths: ['.github/workflows/jk-v31-c-register-business-plan.yml']
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/jzk-v3.1-phase3-operations
          fetch-depth: 0
      - name: Register business plan route
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('src/router/modules/jkBusiness.js')
          s=p.read_text(encoding='utf-8')
          anchor="    { path: 'performanceRecord', component: () => import('@/views/jkBusiness/performanceRecord/index'), name: 'jkPerformanceRecord', meta: { title: '业绩明细', section: 'finance' } },"
          line="    { path: 'businessPlan', component: () => import('@/views/jkBusiness/businessPlan/index'), name: 'jkBusinessPlan', meta: { title: '商业方案', section: 'finance' } },\n"
          if "path: 'businessPlan'" not in s:
              if anchor not in s: raise SystemExit('route anchor missing')
              s=s.replace(anchor,line+anchor,1)
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit and remove workflow
        run: |
          rm -f .github/workflows/jk-v31-c-register-business-plan.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/router/modules/jkBusiness.js .github/workflows/jk-v31-c-register-business-plan.yml
          git commit -m 'feat(jzk-v3.1-c): 注册商业方案管理路由'
          git push origin HEAD:feat/jzk-v3.1-phase3-operations
